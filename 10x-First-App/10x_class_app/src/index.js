import React from 'react';
import ReactDOM from 'react-dom/client';
import Button from './Components/button';
const root = ReactDOM.createRoot(document.getElementById('root')); //create element convert jsx in plain html
root.render(
  <React.StrictMode>
    <Button></Button>
  </React.StrictMode>
);

